# Dharklancing-
This is a freelancing website
# প্রজেক্ট ফোল্ডার তৈরি করুন mkdir dharklancing cd dharklancing # Git রিপোজিটরি শুরু করুন git init git config user.email "আপনার_ইমেইল@gmail.com" git config user.name "xrahatffvai52-spec" # Backend সেটআপ mkdir -p backend/{models,routes,config,middleware} cd backend # Backend প্যাকেজ ইনস্টল করুন npm init -y npm install express mongoose bcryptjs jsonwebtoken dotenv cors multer axios socket.io npm install --save-dev nodemon # Environment file তৈরি করুন cat > .env << 'EOF' PORT=5000 MONGODB_URI=mongodb://localhost:27017/dharklancing JWT_SECRET=your_super_secret_jwt_key_2024 NODE_ENV=development # Bkash BKASH_APP_KEY=your_bkash_key BKASH_APP_SECRET=your_bkash_secret BKASH_USERNAME=your_username BKASH_PASSWORD=your_password # Nagad NAGAD_MERCHANT_ID=your_merchant_id NAGAD_MERCHANT_KEY=your_merchant_key EOF cd .. # Frontend সেটআপ npx create-react-app frontend cd frontend npm install axios react-router-dom react-icons react-toastify tailwindcss cd ..
